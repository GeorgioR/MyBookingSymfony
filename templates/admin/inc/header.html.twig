 <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="{{path('homepage')}}">MyBooking</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a class="nav-link" href="{{path('admin_dashboard')}}"> <i class="fas fa-chart-line"></i> Tableau de bord</a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link" href="{{path('admin_ads_list')}}"> <i class="fas fa-hotel"></i> Annonces</a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link" href="{{path('admin_bookings_list')}}"> <i class="fas fa-folder"></i> Reservations</a>
                        </li>
                         <li class="nav-item active">
                            <a class="nav-link" href="{{path('admin_comments_list')}}"><i class="fas fa-comments"></i> Commentaires</a>
                        </li>
                         <li class="nav-item active">
                            <a class="nav-link" href="#"> <i class="fas fa-users"></i>Utilisateurs</a>
                        </li>
                    </ul>
                        
                    <ul class="navbar-nav ml-auto align-items-center">

                    {% if app.user %}

                        <li class="nav-item ">
                            <a href="{{path('homepage')}}" class="nav-link text-dark" target="_blank">Voir le site</a>
                        </li>

                        <li class="nav-item dropdon">

                            <a href="" class="nav-link dropdown-toggle" id="accountDropdownLink" data-toggle="dropdown">
                                <img src="{{app.user.avatar}}" alt="Avatar de {{ app.user.fullName }}" class="avatar avatar-small">
                                {{app.user.fullName}}
                            
                            </a>

                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                            
                        
                                
                                <a class="dropdown-item" href="{{path('admin_account_logout')}}">Deconnexion</a>
                                
                            </div>

                            

                        </li>
                    
                    {% else %}
                        <li class="nav-item active">
                            <a class="nav-link" href="{{path('account_login')}}">Connexion</a>
                        </li>

                        <li class="nav-item active">
                            <a class="nav-link" href="{{path('account_register')}}">Inscription</a>
                        </li>
                    {% endif %}
                    </ul>
                    
                </div>
        </nav>